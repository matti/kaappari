#!/usr/bin/env bash

if [[ $# -le 1 || ( $# == "--help") ||  $# == "-h" ]]; then
	echo "usage: kaappari http://www.example.com example.png"
	exit 1
fi

node_modules/phantomjs/bin/phantomjs runner.js $1 $2
